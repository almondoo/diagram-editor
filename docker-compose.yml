services:
  app:
    build: .
    working_dir: /app
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    ports:
      - "5173:5173"
      - "4173:4173"
    tty: true
    stdin_open: true
volumes:
  node_modules:
