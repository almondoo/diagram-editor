# エッジドラッグ機能 実装プラン

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** エッジをドラッグで移動・接続先付け替えできるようにし、不要になった自動迂回ルーティングを削除する

**Architecture:** 3段階で実装する。(1) 自動迂回ルーティング削除 (2) エッジ全体のドラッグ移動 (3) 端点ドラッグによる接続先付け替え。既存の `useNodeDrag` パターン(window イベントリスナー方式)を踏襲する。

**Tech Stack:** React, TypeScript, SVG

---

### Task 1: 自動迂回ルーティング削除 — core パッケージ

**Files:**
- Modify: `packages/core/src/types.ts:33-34`
- Modify: `packages/core/src/geometry.ts:1-147`
- Modify: `packages/core/src/index.ts:17`
- Modify: `packages/core/src/svg-export.ts:74`
- Modify: `packages/core/src/__tests__/geometry.test.ts`

**Step 1: `types.ts` から `_routePoints` を削除**

`packages/core/src/types.ts` の `DiagramEdge` インターフェースから以下を削除:

```typescript
  /** ランタイム専用: エッジ迂回ウェイポイント（DSLには保存しない） */
  _routePoints?: Array<{ x: number; y: number }>;
```

**Step 2: `geometry.ts` から `computeEdgeRoute`, `segmentIntersectsRect`, `catmullRomPath` を削除し、`buildEdgePath` から `routePoints` パラメータを削除**

`packages/core/src/geometry.ts` を以下のように変更:

- 行 4-34 の `segmentIntersectsRect` 関数を削除
- 行 40-82 の `computeEdgeRoute` 関数を削除
- 行 88-103 の `catmullRomPath` 関数を削除
- 行 108-147 の `buildEdgePath` を以下に置換:

```typescript
export function buildEdgePath(
  from: { x: number; y: number },
  to: { x: number; y: number },
  curve: string,
): { pathD: string; labelX: number; labelY: number } {
  const midX = (from.x + to.x) / 2;
  const midY = (from.y + to.y) / 2;
  const dx = to.x - from.x;
  const dy = to.y - from.y;
  const perpX = -dy * 0.08;
  const perpY = dx * 0.08;

  if (curve === "straight") {
    return {
      pathD: `M${from.x},${from.y} L${to.x},${to.y}`,
      labelX: midX,
      labelY: midY,
    };
  }

  return {
    pathD: `M${from.x},${from.y} Q${midX + perpX},${midY + perpY} ${to.x},${to.y}`,
    labelX: midX + perpX / 2,
    labelY: midY + perpY / 2,
  };
}
```

**Step 3: `index.ts` から `computeEdgeRoute` エクスポートを削除**

`packages/core/src/index.ts` 行 17 を変更:

```typescript
// before
export { getShapePath, getNodeCenter, getEdgePoints, computeEdgeRoute, buildEdgePath } from "./geometry.js";
// after
export { getShapePath, getNodeCenter, getEdgePoints, buildEdgePath } from "./geometry.js";
```

**Step 4: `svg-export.ts` から `_routePoints` 参照を削除**

`packages/core/src/svg-export.ts` 行 74:

```typescript
// before
const { pathD, labelX, labelY } = buildEdgePath(from, to, edge.curve, edge._routePoints);
// after
const { pathD, labelX, labelY } = buildEdgePath(from, to, edge.curve);
```

**Step 5: テストから `computeEdgeRoute` と `routePoints` 関連テストを削除**

`packages/core/src/__tests__/geometry.test.ts` から:
- import の `computeEdgeRoute` を削除
- `describe("computeEdgeRoute", ...)` ブロック全体を削除
- `buildEdgePath` テスト内の `routePoints` を使うテストケースを削除(Catmull-Rom 関連)

**Step 6: テスト実行**

Run: `docker compose exec app pnpm --filter diagram-dsl-core test`
Expected: PASS

**Step 7: コミット**

```bash
git add packages/core/src/types.ts packages/core/src/geometry.ts packages/core/src/index.ts packages/core/src/svg-export.ts packages/core/src/__tests__/geometry.test.ts
git commit -m "refactor: 自動迂回ルーティング(computeEdgeRoute/_routePoints)を削除"
```

---

### Task 2: 自動迂回ルーティング削除 — react パッケージ

**Files:**
- Modify: `packages/react/src/hooks/useDiagramState.ts:6-9, 145-159`
- Modify: `packages/react/src/components/EdgeLine.tsx:15,18`

**Step 1: `useDiagramState.ts` から迂回計算を削除**

import から `computeEdgeRoute` を削除(行 9):

```typescript
// before
import {
  parseDSL,
  autoLayout,
  formatDSLCode,
  generateExportSVG,
  randomColor,
  getEdgePoints,
  computeEdgeRoute,
} from "diagram-dsl-core";
// after
import {
  parseDSL,
  autoLayout,
  formatDSLCode,
  generateExportSVG,
  randomColor,
  getEdgePoints,
} from "diagram-dsl-core";
```

`routedEdges` useMemo (行 145-159) を簡略化:

```typescript
  // before: エッジルーティング計算
  // after: parsedRaw.edges をそのまま使用（routedEdges 変数自体を削除）
```

`routedEdges` を参照している `parsed` useMemo (行 163) で `routedEdges` を `parsedRaw.edges` に置換。`routedEdges` の useMemo ブロック全体を削除。

**Step 2: `EdgeLine.tsx` から `_routePoints` 参照を削除**

```typescript
// before (行 15)
const { label, color, style, animate, thickness, arrow, curve, _routePoints } = edge;
// after
const { label, color, style, animate, thickness, arrow, curve } = edge;

// before (行 18)
const { pathD, labelX, labelY } = buildEdgePath(from, to, curve, _routePoints);
// after
const { pathD, labelX, labelY } = buildEdgePath(from, to, curve);
```

**Step 3: 型チェック → ビルド**

Run: `docker compose exec app pnpm --filter diagram-dsl-core build && docker compose exec app pnpm --filter diagram-dsl-react build && docker compose exec app pnpm -r typecheck`
Expected: PASS

**Step 4: コミット**

```bash
git add packages/react/src/hooks/useDiagramState.ts packages/react/src/components/EdgeLine.tsx
git commit -m "refactor: react パッケージから迂回ルーティング参照を削除"
```

---

### Task 3: useEdgeDrag フック作成 — エッジ全体移動

**Files:**
- Create: `packages/react/src/hooks/useEdgeDrag.ts`

**Step 1: `useEdgeDrag` フックを作成**

`packages/react/src/hooks/useEdgeDrag.ts`:

```typescript
import { useState, useEffect, useCallback, useRef } from "react";
import type { DiagramNode, DiagramEdge } from "diagram-dsl-core";

interface EdgeDragInfo {
  type: "move";
  fromId: string;
  toId: string;
  startX: number;
  startY: number;
}

interface ReconnectDragInfo {
  type: "reconnect";
  /** ドラッグ中の端: "from" = 始点を付け替え, "to" = 終点を付け替え */
  end: "from" | "to";
  /** 元の edge の from/to */
  originalFrom: string;
  originalTo: string;
  /** 固定端のノードID */
  anchorId: string;
  /** 現在のカーソル位置(SVG座標) */
  cursorX: number;
  cursorY: number;
  startX: number;
  startY: number;
}

type DragInfo = EdgeDragInfo | ReconnectDragInfo;

export function useEdgeDrag(
  nodeById: Record<string, DiagramNode>,
  zoom: number,
  setNodeLayout: (nodeId: string, x: number, y: number) => void,
  reconnectEdge: (originalFrom: string, originalTo: string, newFrom: string, newTo: string) => void,
  svgRef: React.RefObject<SVGSVGElement | null>,
  panRef: React.RefObject<{ x: number; y: number }>,
) {
  const [dragInfo, setDragInfo] = useState<DragInfo | null>(null);
  const nodeByIdRef = useRef(nodeById);
  nodeByIdRef.current = nodeById;

  // エッジの線をドラッグ開始(全体移動)
  const handleEdgeMoveMouseDown = useCallback((e: React.MouseEvent, fromId: string, toId: string) => {
    e.stopPropagation();
    if (e.button !== 0) return;
    setDragInfo({ type: "move", fromId, toId, startX: e.clientX, startY: e.clientY });
  }, []);

  // 端点のドラッグ開始(接続先付け替え)
  const handleEdgeEndpointMouseDown = useCallback((e: React.MouseEvent, fromId: string, toId: string, end: "from" | "to") => {
    e.stopPropagation();
    if (e.button !== 0) return;
    const svgEl = svgRef.current;
    if (!svgEl) return;
    const rect = svgEl.getBoundingClientRect();
    const cursorX = (e.clientX - rect.left - panRef.current.x) / zoom;
    const cursorY = (e.clientY - rect.top - panRef.current.y) / zoom;
    setDragInfo({
      type: "reconnect",
      end,
      originalFrom: fromId,
      originalTo: toId,
      anchorId: end === "from" ? toId : fromId,
      cursorX,
      cursorY,
      startX: e.clientX,
      startY: e.clientY,
    });
  }, [svgRef, panRef, zoom]);

  useEffect(() => {
    if (!dragInfo) return;

    const handleMove = (e: MouseEvent) => {
      if (dragInfo.type === "move") {
        const dx = (e.clientX - dragInfo.startX) / zoom;
        const dy = (e.clientY - dragInfo.startY) / zoom;
        if (Math.abs(dx) < 1 && Math.abs(dy) < 1) return;

        const fromNode = nodeByIdRef.current[dragInfo.fromId];
        const toNode = nodeByIdRef.current[dragInfo.toId];
        if (fromNode) setNodeLayout(dragInfo.fromId, Math.round(fromNode.x + dx), Math.round(fromNode.y + dy));
        if (toNode) setNodeLayout(dragInfo.toId, Math.round(toNode.x + dx), Math.round(toNode.y + dy));
        setDragInfo((d) => d ? { ...d, startX: e.clientX, startY: e.clientY } : null);
      } else if (dragInfo.type === "reconnect") {
        const svgEl = svgRef.current;
        if (!svgEl) return;
        const rect = svgEl.getBoundingClientRect();
        const cursorX = (e.clientX - rect.left - panRef.current.x) / zoom;
        const cursorY = (e.clientY - rect.top - panRef.current.y) / zoom;
        setDragInfo((d) => d && d.type === "reconnect" ? { ...d, cursorX, cursorY } : d);
      }
    };

    const handleUp = (e: MouseEvent) => {
      if (dragInfo.type === "reconnect") {
        // ドロップ先のノードを検出
        const svgEl = svgRef.current;
        if (svgEl) {
          const rect = svgEl.getBoundingClientRect();
          const cx = (e.clientX - rect.left - panRef.current.x) / zoom;
          const cy = (e.clientY - rect.top - panRef.current.y) / zoom;

          let targetNodeId: string | null = null;
          for (const [id, node] of Object.entries(nodeByIdRef.current)) {
            if (id === dragInfo.anchorId) continue;
            if (cx >= node.x && cx <= node.x + node.w && cy >= node.y && cy <= node.y + node.h) {
              targetNodeId = id;
              break;
            }
          }

          if (targetNodeId) {
            const newFrom = dragInfo.end === "from" ? targetNodeId : dragInfo.originalFrom;
            const newTo = dragInfo.end === "to" ? targetNodeId : dragInfo.originalTo;
            if (newFrom !== newTo) {
              reconnectEdge(dragInfo.originalFrom, dragInfo.originalTo, newFrom, newTo);
            }
          }
        }
      }
      setDragInfo(null);
    };

    window.addEventListener("mousemove", handleMove);
    window.addEventListener("mouseup", handleUp);
    return () => {
      window.removeEventListener("mousemove", handleMove);
      window.removeEventListener("mouseup", handleUp);
    };
  }, [dragInfo, zoom, setNodeLayout, reconnectEdge, svgRef, panRef]);

  return {
    edgeDragInfo: dragInfo,
    handleEdgeMoveMouseDown,
    handleEdgeEndpointMouseDown,
  };
}
```

**Step 2: 型チェック**

Run: `docker compose exec app pnpm --filter diagram-dsl-react build && docker compose exec app pnpm -r typecheck`
Expected: PASS

**Step 3: コミット**

```bash
git add packages/react/src/hooks/useEdgeDrag.ts
git commit -m "feat: useEdgeDrag フックを作成(エッジ移動+接続付け替え)"
```

---

### Task 4: EdgeLine にヒットエリア・端点ハンドル追加

**Files:**
- Modify: `packages/react/src/components/EdgeLine.tsx`

**Step 1: `EdgeLine` にドラッグ用 UI を追加**

`packages/react/src/components/EdgeLine.tsx` を全面書き換え:

```tsx
import { memo, useState } from "react";
import type { DiagramNode, DiagramEdge } from "diagram-dsl-core";
import { getEdgePoints, buildEdgePath, getNodeCenter } from "diagram-dsl-core";

interface EdgeLineProps {
  edge: DiagramEdge;
  fromNode: DiagramNode | undefined;
  toNode: DiagramNode | undefined;
  onMoveMouseDown?: (e: React.MouseEvent, fromId: string, toId: string) => void;
  onEndpointMouseDown?: (e: React.MouseEvent, fromId: string, toId: string, end: "from" | "to") => void;
}

export const EdgeLine = memo(
  function EdgeLine({ edge, fromNode, toNode, onMoveMouseDown, onEndpointMouseDown }: EdgeLineProps) {
    if (!fromNode || !toNode) return null;
    const { from, to } = getEdgePoints(fromNode, toNode);
    const { label, color, style, animate, thickness, arrow, curve } = edge;
    const id = `edge-${edge.from}-${edge.to}`;
    const [hovered, setHovered] = useState(false);

    const { pathD, labelX, labelY } = buildEdgePath(from, to, curve);

    const hasEndMarker = arrow === "end" || arrow === "both";
    const hasStartMarker = arrow === "start" || arrow === "both";

    return (
      <g
        onMouseEnter={() => setHovered(true)}
        onMouseLeave={() => setHovered(false)}
      >
        <defs>
          {hasEndMarker && (
            <marker
              id={`ah-${id}`}
              markerWidth="10"
              markerHeight="7"
              refX="9"
              refY="3.5"
              orient="auto"
            >
              <polygon points="0 0, 10 3.5, 0 7" fill={color} />
            </marker>
          )}
          {hasStartMarker && (
            <marker
              id={`ah-start-${id}`}
              markerWidth="10"
              markerHeight="7"
              refX="1"
              refY="3.5"
              orient="auto"
            >
              <polygon points="10 0, 0 3.5, 10 7" fill={color} />
            </marker>
          )}
        </defs>

        {/* 透明なヒットエリア(太いパスで掴みやすくする) */}
        <path
          d={pathD}
          fill="none"
          stroke="transparent"
          strokeWidth={14}
          style={{ cursor: "move" }}
          onMouseDown={(e) => onMoveMouseDown?.(e, edge.from, edge.to)}
        />

        {/* 実際のエッジ線 */}
        <path
          d={pathD}
          fill="none"
          stroke={color}
          strokeWidth={thickness}
          strokeDasharray={style === "dashed" ? "8,4" : "none"}
          markerEnd={hasEndMarker ? `url(#ah-${id})` : undefined}
          markerStart={hasStartMarker ? `url(#ah-start-${id})` : undefined}
          className={animate ? "edge-animate" : ""}
          style={{ pointerEvents: "none" }}
        />

        {/* ラベル */}
        {label && (
          <g style={{ pointerEvents: "none" }}>
            <rect
              x={labelX - label.length * 4 - 4}
              y={labelY - 10}
              width={label.length * 8 + 8}
              height={20}
              rx={4}
              fill="#0f172a"
              fillOpacity={0.85}
            />
            <text
              x={labelX}
              y={labelY + 1}
              textAnchor="middle"
              dominantBaseline="middle"
              fill="#e2e8f0"
              fontSize={11}
              fontFamily="'IBM Plex Mono', monospace"
              fontWeight="500"
            >
              {label}
            </text>
          </g>
        )}

        {/* 端点ドラッグハンドル(ホバー時のみ表示) */}
        {hovered && (
          <>
            <circle
              cx={from.x}
              cy={from.y}
              r={5}
              fill="#6366f1"
              stroke="#fff"
              strokeWidth={1.5}
              style={{ cursor: "crosshair" }}
              onMouseDown={(e) => onEndpointMouseDown?.(e, edge.from, edge.to, "from")}
            />
            <circle
              cx={to.x}
              cy={to.y}
              r={5}
              fill="#6366f1"
              stroke="#fff"
              strokeWidth={1.5}
              style={{ cursor: "crosshair" }}
              onMouseDown={(e) => onEndpointMouseDown?.(e, edge.from, edge.to, "to")}
            />
          </>
        )}
      </g>
    );
  },
  (prev, next) =>
    prev.edge === next.edge &&
    prev.fromNode === next.fromNode &&
    prev.toNode === next.toNode &&
    prev.onMoveMouseDown === next.onMoveMouseDown &&
    prev.onEndpointMouseDown === next.onEndpointMouseDown,
);
```

**Step 2: 型チェック → ビルド**

Run: `docker compose exec app pnpm --filter diagram-dsl-react build && docker compose exec app pnpm -r typecheck`
Expected: PASS

**Step 3: コミット**

```bash
git add packages/react/src/components/EdgeLine.tsx
git commit -m "feat: EdgeLine にヒットエリアと端点ドラッグハンドルを追加"
```

---

### Task 5: useDiagramState に reconnectEdge を追加

**Files:**
- Modify: `packages/react/src/hooks/useDiagramState.ts`

**Step 1: `reconnectEdge` 関数を追加**

`deleteEdge` の後(行 467 付近)に追加:

```typescript
  // エッジの接続先を付け替え（DSLコード内の edge 行を書き換え）
  const reconnectEdge = useCallback((originalFrom: string, originalTo: string, newFrom: string, newTo: string) => {
    setCode((c) => {
      const lines = c.split("\n");
      // 全オペレータに対応する正規表現
      const edgePattern = new RegExp(
        `^(\\s*edge\\s+)${originalFrom}(\\s*(?:<-->|<->|<--|-->|<-|->|--)\\s*)${originalTo}(\\s|$)(.*)`
      );
      const updated = lines.map((line) => {
        const match = line.match(edgePattern);
        if (!match) return line;
        return `${match[1]}${newFrom}${match[2]}${newTo}${match[3]}${match[4] ?? ""}`;
      });
      return updated.join("\n");
    });
  }, []);
```

**Step 2: `DiagramState` インターフェースに `reconnectEdge` を追加**

```typescript
// DiagramState interface に追加
reconnectEdge: (originalFrom: string, originalTo: string, newFrom: string, newTo: string) => void;
```

**Step 3: return 文に `reconnectEdge` を追加**

```typescript
  return {
    // ... 既存の返り値
    reconnectEdge,
  };
```

**Step 4: 型チェック → ビルド**

Run: `docker compose exec app pnpm --filter diagram-dsl-react build && docker compose exec app pnpm -r typecheck`
Expected: PASS

**Step 5: コミット**

```bash
git add packages/react/src/hooks/useDiagramState.ts
git commit -m "feat: reconnectEdge 関数を追加(エッジ接続先付け替え)"
```

---

### Task 6: DiagramEditor に useEdgeDrag を組み込み

**Files:**
- Modify: `packages/react/src/DiagramEditor.tsx`

**Step 1: import と hook の追加**

import に追加:

```typescript
import { useEdgeDrag } from "./hooks/useEdgeDrag.js";
```

`state` のデストラクチャリングに `reconnectEdge` を追加:

```typescript
  const {
    code, setCode, parsed, nodeById, groupById, noteStates,
    setNodeLayout, setNodeSize, setGroupLayout, setGroupSize, setNoteLayout, multiMoveLayout,
    addNode, addNote, addEdge, updateNodeProp, updateEdgeProp, deleteEdge, deleteNode, reconnectEdge, exportSVG, formatCode, resetLayout,
  } = state;
```

既存のフック群の後(行 166 付近)に追加:

```typescript
  const { edgeDragInfo, handleEdgeMoveMouseDown, handleEdgeEndpointMouseDown } =
    useEdgeDrag(nodeById, zoom, setNodeLayout, reconnectEdge, svgRef, panRef);
```

**Step 2: EdgeLine の描画部分にコールバックを渡す**

`renderCanvas` 内の EdgeLine 描画(行 329-336)を変更:

```tsx
          {parsed.edges.map((edge, i) => (
            <EdgeLine
              key={`${edge.from}-${edge.to}-${i}`}
              edge={edge}
              fromNode={nodeById[edge.from]}
              toNode={nodeById[edge.to]}
              onMoveMouseDown={handleEdgeMoveMouseDown}
              onEndpointMouseDown={handleEdgeEndpointMouseDown}
            />
          ))}
```

**Step 3: 接続付け替えの仮エッジ線を描画**

EdgeLine 一覧の後、ノード描画の前に追加:

```tsx
          {/* 接続付け替え中の仮エッジ線 */}
          {edgeDragInfo?.type === "reconnect" && (() => {
            const anchorNode = nodeById[edgeDragInfo.anchorId];
            if (!anchorNode) return null;
            const ac = { x: anchorNode.x + anchorNode.w / 2, y: anchorNode.y + anchorNode.h / 2 };
            const cursor = { x: edgeDragInfo.cursorX, y: edgeDragInfo.cursorY };
            const from = edgeDragInfo.end === "from" ? cursor : ac;
            const to = edgeDragInfo.end === "from" ? ac : cursor;
            return (
              <line
                x1={from.x} y1={from.y}
                x2={to.x} y2={to.y}
                stroke="#6366f1"
                strokeWidth={2}
                strokeDasharray="6,3"
                style={{ pointerEvents: "none" }}
              />
            );
          })()}
```

**Step 4: 型チェック → ビルド**

Run: `docker compose exec app pnpm --filter diagram-dsl-react build && docker compose exec app pnpm -r typecheck`
Expected: PASS

**Step 5: コミット**

```bash
git add packages/react/src/DiagramEditor.tsx
git commit -m "feat: DiagramEditor に useEdgeDrag を組み込み"
```

---

### Task 7: 全体ビルド・手動テスト

**Step 1: 全パッケージビルド**

Run: `docker compose exec app pnpm -r build`
Expected: PASS

**Step 2: 型チェック**

Run: `docker compose exec app pnpm -r typecheck`
Expected: PASS

**Step 3: core テスト**

Run: `docker compose exec app pnpm --filter diagram-dsl-core test`
Expected: PASS

**Step 4: ブラウザで手動テスト**

http://localhost:5173 で以下を確認:
- エッジの線をドラッグ → 両端ノードが一緒に移動する
- エッジにホバー → 端点にハンドル(青い丸)が表示される
- 端点ハンドルをドラッグ → 仮のエッジ線がカーソルに追従
- 別ノード上でドロップ → DSLコード内の接続先が書き換わる
- 空白エリアでドロップ → キャンセル(何も起きない)
- 自動迂回が無くなっている(エッジがノードを貫通する場合がある)

**Step 5: コミット(必要な修正がある場合)**

```bash
git add -A
git commit -m "fix: エッジドラッグ機能の調整"
```
